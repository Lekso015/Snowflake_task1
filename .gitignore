# Airflow runtime data (regenerated automatically, not needed in repo)
airflow/logs/
airflow/plugins/
airflow/config/

# Airflow secrets — commit .env.example instead, never the real .env
airflow/.env

# Large source data file
airline_data.csv
*.csv

# Python
__pycache__/
*.pyc